// امر الباندل الخاص بالبوت
pybabel extract . -F babel.cfg -o bot/locales/messages.pot
pybabel compile -d bot/locales -D messages
# إنشاء ملف الترجمة للغة العربية
pybabel init -i bot/locales/messages.pot -d bot/locales -l ar

# للغة الإنجليزية (اختياري)
pybabel init -i bot/locales/messages.pot -d bot/locales -l en
تعديل السابق
pybabel update -i bot/locales/messages.pot -d bot/locales

================================================================================
# 1. استخرج النصوص الجديدة (هذا يجدد messages.pot)
pybabel extract . -F babel.cfg -o bot/locales/messages.pot

# 2. قم بتحديث ملفات الترجمة الموجودة (هذا يضيف النصوص الجديدة ويحتفظ بالترجمات القديمة)
pybabel update -i bot/locales/messages.pot -d bot/locales

# 3. الآن افتح ملفات .po وقم بترجمة النصوص الجديدة فقط
# (النصوص القديمة محفوظة ولن تضيع)

# 4. بعد الانتهاء من الترجمة، قم بتجميع
pybabel compile -d bot/locales
================================================================================
// امر اعتماد الباندل في جانقو
python manage.py makemessages -l ar -i accessories -i venv
python manage.py compilemessages


python manage.py makemigrations
python manage.py migrate
// امر حذف القاعدة بالكامل
del db.sqlite3 ; Get-ChildItem -Path "apps" -Filter "*.py" -Recurse | Where-Object { $_.DirectoryName -like "*\migrations*" -and $_.Name -ne "__init__.py" } | Remove-Item
// هذا في bash
rm -f db.sqlite3 && find apps -path "*/migrations/*.py" -not -name "__init__.py" -delete



python -m bot.main